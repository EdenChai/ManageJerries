#ifndef DEFS_H
#define DEFS_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "Jerry.h"

typedef enum e_bool {false, true} bool;
typedef enum e_status {success, failure} status;

#endif /* DEFS_H */
